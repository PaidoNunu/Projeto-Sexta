{
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "name": "fim",
  "version": "1.0.0",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "concurrently \"cd servidor && npm start\" \"cd cliente && npm start\"",
    "update": "concurrently \"cd servidor && npm i\" \"cd cliente && npm i\""
  },
  "author": "",
  "license": "ISC",
  "description": ""
}
